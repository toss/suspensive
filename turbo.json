{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build:lib"],
      "outputs": ["dist/**", ".next/**"]
    },
    "build:lib": {
      "dependsOn": ["^build:lib"],
      "outputs": ["dist/**"]
    },
    "ci:eslint": {
      "dependsOn": ["build:lib"]
    },
    "ci:test": {
      "dependsOn": ["build:lib"],
      "outputs": ["coverage/**"]
    },
    "ci:type": {
      "dependsOn": ["build:lib"]
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "dependsOn": ["build:lib"],
      "outputs": ["dist/**", "coverage/**"],
      "cache": false,
      "persistent": true
    }
  },
  "globalEnv": ["NODE_ENV"]
}
