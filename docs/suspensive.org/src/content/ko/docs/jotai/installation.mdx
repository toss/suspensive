import { Steps } from 'nextra/components'

# 설치하기

<Steps>

### 지원 브라우저 확인하기

Suspensive 라이브러리는 모던 브라우저를 위해 최적화되었습니다. 아래 브라우저에 호환되도록 설정되어 있습니다.

<br />

<table>
  <tbody>
    {[
      { browser: 'Chrome', version: '>= 51' },
      { browser: 'Firefox', version: '>= 53' },
      { browser: 'Edge', version: '>= 18' },
      { browser: 'Safari', version: '>= 11' },
      { browser: 'iOS', version: '>= 11' },
      { browser: 'Opera', version: '>= 38' },
    ].map((item) => (
      <tr key={item.browser}>
        <td>{item.browser}</td>
        <td className="text-center">{item.version}</td>
      </tr>
    ))}
  </tbody>
</table>

> 당신의 환경에 따라, polyfills추가가 필요할 수도 있습니다. 더 오래된 브라우저를 지원하려면, 스스로 node_modules에서 라이브러리를 transpile할 필요가 있습니다.

### react, jotai 버전 확인하기

<br />

<table>
  <tbody>
    {[
      { library: 'react', version: '^18 || ^19' },
      { library: 'jotai', version: '^2' },
    ].map((item) => (
      <tr key={item.library}>
        <td>{item.library}</td>
        <td className="text-center">{item.version}</td>
      </tr>
    ))}
  </tbody>
</table>

### 의존성 추가

@suspensive/jotai는 npm에 있습니다. 최신 안정버전을 설치하기 위해 아래 커맨드를 실행하세요.

[![npm version](https://img.shields.io/npm/v/@suspensive/jotai?color=000&labelColor=000&logo=npm&label=)](https://www.npmjs.com/package/@suspensive/jotai)

```shell npm2yarn
npm install @suspensive/jotai
```

</Steps>
